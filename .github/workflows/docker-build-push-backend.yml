# Name of the entire GitHub Actions workflow. This is displayed in the "Actions" tab.
name: Build and Push Backend Docker Image

# --- Triggers ---
# Defines the events that will trigger this workflow to run.
on:
  # Allows manual triggering of the workflow from the GitHub Actions tab.
  workflow_dispatch:
  # Trigger the workflow on push events
  push:
    # Only for pushes to the main and develop branches
    branches:
      - main
      - develop
    # Only if changes are detected in these specific paths
    paths:
      - "backend/**"
      - ".github/workflows/docker-build-push-backend.yml"
  # Also trigger on pull request events
  pull_request:
    # Only for pull requests targeting the main and develop branches
    branches:
      - main
      - develop
    paths:
      - "backend/**"
      - ".github/workflows/docker-build-push-backend.yml"

# --- Environment Variables ---
# Variables available to all jobs in this workflow.
env:
  REGISTRY: ghcr.io # Specifies the Docker registry to use (GitHub Container Registry)

# --- Jobs ---
# A workflow run is made up of one or more jobs, which run in parallel by default.
jobs:
  # The single job in this workflow, responsible for building the backend image.
  build:
    # The type of runner that the job will run on.
    runs-on: ubuntu-latest
    # Permissions granted to the GITHUB_TOKEN for this job.
    permissions:
      contents: read # Required to check out the repository code.
      packages: write # Required to push Docker images to a registry like Docker Hub.

    # --- Steps ---
    # A sequence of tasks that will be executed as part of the job.
    steps:
      # Step 1: Check out the repository's code so the job can access it.
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true # Required to fetch BuildingBlocks submodule

      # Step 2: Create .env file for tests
      - name: Create .env file for tests
        working-directory: ./backend
        run: |
          echo "Creating .env file from variables..."
          echo "${{ vars.ENV_FILE }}" > .env
          echo "${{ secrets.ENV_FILE }}" >> .env

      # Step 3: Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      # Step 4: Configure NuGet authentication for GitHub Packages
      - name: Configure NuGet for GitHub Packages
        working-directory: ./backend
        run: |
          dotnet nuget update source github \
            --source "https://nuget.pkg.github.com/bswierzewski/index.json" \
            --username ${{ github.actor }} \
            --password ${{ secrets.GITHUB_TOKEN }} \
            --store-password-in-clear-text

      # Step 5: Restore dependencies with GitHub token authentication
      - name: Restore dependencies
        working-directory: ./backend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: dotnet restore

      # Step 6: Build solution
      - name: Build solution
        working-directory: ./backend
        run: dotnet build --no-restore --configuration Release

      # Step 7: Run E2E Tests (before building Docker image)
      - name: Run E2E Tests
        working-directory: ./backend
        run: dotnet test tests/Snippet.Tests.EndToEnd/Snippet.Tests.EndToEnd.csproj --no-build --configuration Release --verbosity normal

      # Step 8: Remove .env file after build
      - name: Remove .env file
        working-directory: ./backend
        run: rm -f .env

      # Step 9: Set up Docker Buildx, an advanced builder that enables features like caching.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Step 10: Log in to the Docker registry (GitHub Container Registry).
      - name: Log in to GitHub Container Registry
        # This step only runs on 'push' events, not on 'pull_request' events.
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }} # GitHub username from the workflow context.
          password: ${{ secrets.GITHUB_TOKEN }} # GitHub token automatically provided by Actions.

      # Step 11: Automatically generate tags and labels for the Docker image.
      - name: Extract metadata (tags, labels)
        id: meta # Assign an ID to reference its outputs.
        uses: docker/metadata-action@v5
        with:
          # The full image name for backend service.
          images: ${{ env.REGISTRY }}/${{ github.repository }}-backend
          # Rules for generating tags based on Git events.
          tags: |
            type=ref,event=branch       # Tag with branch name (e.g., 'main')
            type=ref,event=pr           # Tag with pull request number (e.g., 'pr-123')
            type=sha,prefix={{branch}}- # Tag with branch and short commit SHA (e.g., 'main-a1b2c3d')
            type=raw,value=latest,enable={{is_default_branch}} # Tag 'latest' only for the default branch (usually 'main')

      # Step 12: Build the Docker image and push it to the registry.
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          # The build context (directory) for backend.
          context: ./backend
          # The path to the Dockerfile.
          file: ./backend/Dockerfile
          # Push the image only on 'push' events, not on 'pull_request' events.
          push: ${{ github.event_name != 'pull_request' }}
          # Use the tags and labels generated by the 'meta' step.
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Use GitHub Actions cache to speed up future builds.
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Pass build arguments for backend
          build-args: |
            GIT_COMMIT_SHA=${{ github.sha }}
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
